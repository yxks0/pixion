specVersion: "v1"
serviceId: "pixionapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pixionapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
